<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="styles.css">
  <title>{{ title }}</title>
</head>
<body>
  <h1>{{ heading }}</h1>
  <ul>
    {% for feature in features %}
    <li>
      <h2>{{ feature.name }}</h2>
      <p>Status: 
        {% if feature.status.baseline == 'high' %}
          Widely Available
        {% elif feature.status.baseline == 'low' %}
          Newly Available
        {% else %}
          {{ feature.status.baseline | capitalize }}
        {% endif %}
      </p>
      <p>Description: {{ feature.description }}</p>
    </li>
    {% endfor %}
  </ul>
  <a href="index.html">Back to index</a>
</body>
</html>
